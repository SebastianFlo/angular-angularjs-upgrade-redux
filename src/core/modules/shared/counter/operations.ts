/** @format */

/**
 * Internal dependancies
 */
import * as actions from './actions';

/**
 * Fetches a paginated list of feeds
 *
 * @param {types.FeedPagination} pagination
 */
export const increaseCounter = () => actions.increaseCounter();
export const decreaseCounter = () => actions.decreaseCounter();
